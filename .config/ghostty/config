# Font settings
font_family = "FiraCode Nerd Font"
font_size = 12.0

# Cursor settings
cursor_blink = true
cursor_shape = "block"

# Theme (use any theme or define your own colors)
foreground_color = "#d0d0d0"
background_color = "#1e1e2e"

# Padding (in pixels)
padding_x = 10
padding_y = 10

# Shell (optional override)
# shell = "/usr/bin/zsh"

# Scrollback lines
scrollback_lines = 10000

# Enable true color
true_color = true
